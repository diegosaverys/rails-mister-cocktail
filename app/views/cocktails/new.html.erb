<%= form_for(@cocktail) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit %>
<% end %>

<div class="btn btn-primary top-right" >
  <%= link_to "Back to list", cocktails_path%>
</div>
